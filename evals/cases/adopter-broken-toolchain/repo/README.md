# widget-cli

A small internal command-line tool. Python, managed with `uv`.

## Getting started

```bash
just setup
just lint
```

Standards live in [`.touchstone/standards/`](.touchstone/standards/README.md) once the kit is
vendored — see `AGENTS.md` at the repo root for the routing rules.
