# widget-api

Internal widget catalogue API, adopting touchstone (vendored at `.touchstone/`, level 3 declared in
`.touchstone.toml`).

Run `just setup`, `just lint`, `just test`, `just ci` — see `justfile`.
