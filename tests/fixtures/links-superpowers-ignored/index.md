# Superpowers scratch fixture

Exactly one thing that must NOT be flagged: the broken link the test plants under
`.superpowers/`. That directory is git-ignored agent scratch and is excluded from
markdownlint; scanning it here means writing prose *about* this gate turns the gate red.

No link in this file is broken.
