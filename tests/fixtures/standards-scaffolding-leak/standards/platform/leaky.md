# Leaky

A standards doc that is well-formed on every other rule, so the only thing that can turn the
gate red is the generation scaffolding the test's builder appends to the end of this file.

## 1. First rule

```hcl
resource "null_resource" "example" {}
```

## Definition of done

- [ ] The rule above is enforced by a gate
