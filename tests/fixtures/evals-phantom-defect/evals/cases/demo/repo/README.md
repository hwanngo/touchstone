# demo repo (fixture)

This fixture repo deliberately has no `src/config.py` — the case's `answer-key.txt` claims a
defect there anyway, which is exactly the phantom `scripts/check-evals.sh` must catch.
