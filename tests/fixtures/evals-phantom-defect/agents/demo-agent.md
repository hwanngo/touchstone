---
name: demo-agent
description: "Use only as fixture data for tests/gates/check-evals.test.sh — never a real agent."
---

# demo-agent

Fixture stub. check-evals.sh only checks that this file exists at the path a case's `meta.txt`
names; it does not validate the file's own shape.
